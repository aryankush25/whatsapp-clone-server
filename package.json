{
  "name": "whatsapp-clone-server",
  "version": "1.0.0",
  "description": "This is a whatsapp server clone.",
  "main": "src/index.js",
  "scripts": {
    "lint": "tslint -p tsconfig.json",
    "clean": "rm -rf build",
    "build": "npm run clean && ./node_modules/.bin/tsc --build",
    "migrate": "npm run build && ./node_modules/typeorm/cli.js migration:run",
    "migrate:generate": "npm run build && ./node_modules/typeorm/cli.js migration:generate -n",
    "start": "npm run migrate && node build/index.js",
    "start:dev": "./node_modules/.bin/tsc --build && node build/index.js",
    "dev": "npm run migrate && nodemon"
  },
  "author": "Aryan Agarwal",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "pg": "^8.5.1",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.2.29",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/node": "^14.14.20",
    "nodemon": "^2.0.7",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.1.3"
  }
}
